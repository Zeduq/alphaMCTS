# AlphaMCTS 可视化 GUI 系统使用说明

## 🚀 快速启动

### 方法1：双击启动（推荐）
```
double_click: run_gui.bat
```

### 方法2：命令行启动
```bash
python mcts_gui.py
```

## 📋 系统要求

- **Python**: 3.7+
- **依赖**: PyQt5
- **操作系统**: Windows 10/11, macOS, Linux

安装依赖：
```bash
pip install PyQt5
```

## 🎨 界面说明

### 左侧面板 - MCTS搜索树

```
┌─────────────────────────────────────┬─────────────┐
│                                     │   节点详情   │
│     [●] ─── [●] ─── [●]            │  ─────────  │
│      │       │       │             │  名称: xxx  │
│     [●]     [●]     [●]            │  Q值: 6.5   │
│      │               │             │  访问: 3    │
│     [●]             [●]            │             │
│                                     │ 五维评分:   │
│  • 鼠标悬停查看详情                  │  • 有效性: 7│
│  • 滚轮缩放                         │  • 稳定性: 6│
│  • 拖动平移                         │  • ...      │
│                                     │             │
│                                     │ 金融指标:   │
│                                     │  • Rank IC  │
│                                     │  • ICIR     │
│                                     │             │
│                                     │ 因子公式:   │
│                                     │ rank(...)   │
└─────────────────────────────────────┴─────────────┘
```

**颜色含义：**
- 🔴 红色 (Q < 3): 质量较差
- 🟡 黄色 (Q 3-5): 质量一般
- 🟢 绿色 (Q 5-7): 质量较好
- 🟢 深绿 (Q >= 7): 质量优秀

### 右侧面板 - 控制与日志

#### 控制面板
| 选项 | 说明 |
|------|------|
| 因子类型 | 选择要挖掘的因子类型（动量/价值/波动率等） |
| 搜索预算 | MCTS迭代次数（建议10-20） |
| 准入阈值 | 入库所需最低Effectiveness分数 |

#### 功能按钮
- **▶ 开始搜索**: 启动MCTS搜索
- **⏹ 停止**: 终止当前搜索
- **🗑 清空树**: 清空可视化树
- **💾 导出结果**: 将入库因子导出为JSON

#### 运行日志
- 实时显示MCTS运行日志
- 支持颜色标记（信息/成功/警告/错误）
- 自动滚动到底部

## 🖱️ 交互操作

### 搜索树操作
| 操作 | 功能 |
|------|------|
| 鼠标悬停 | 显示节点详细信息（Tooltip） |
| 滚轮 | 缩放视图 |
| 左键拖动 | 平移视图 |
| 点击节点 | 在右侧面板显示详情 |

### 节点信息展示
悬停或点击节点时，显示以下信息：

**基本信息：**
- 因子名称
- Q值（综合评分）
- 访问次数

**五维评分：**
- Effectiveness (有效性)
- Stability (稳定性)
- Turnover (换手率)
- Diversity (多样性)
- Overfitting Risk (过拟合风险)

**金融指标：**
- Rank IC
- ICIR
- Sharpe Ratio
- Turnover
- 年化收益
- 最大回撤

**因子公式：**
- 完整的数学表达式

## ⚙️ 使用流程

### 1. 选择因子类型
从下拉菜单选择您感兴趣的因子类型，例如：
- 动量因子
- 价值因子
- 波动率因子

### 2. 设置参数
- **搜索预算**: 决定MCTS迭代次数，建议10-20次
- **准入阈值**: 控制入库标准，建议2-3分

### 3. 开始搜索
点击 **"▶ 开始搜索"** 按钮，系统将：
1. 初始化根节点（生成初始因子）
2. 开始MCTS迭代
3. 实时显示搜索树扩展
4. 将优质因子入库

### 4. 观察过程
- 左侧面板实时显示树的增长
- 右侧面板显示运行日志
- 悬停节点查看详细信息

### 5. 导出结果
搜索完成后，点击 **"💾 导出结果"** 保存入库因子。

## 🔧 高级功能

### 调整视图
- 使用 **鼠标滚轮** 缩放树形图
- 使用 **左键拖动** 平移视图
- 查看不同层级的节点分布

### 节点对比
点击不同节点，在右侧详情面板对比：
- 五维评分差异
- 金融指标差异
- 因子公式差异

## 📝 输出文件

运行结束后，结果保存在：
- `results/mcts_results.json` - 入库因子数据

包含：
- 因子公式
- 五维评分
- 金融指标
- 因子画像

## 🐛 故障排除

### GUI无法启动
```bash
# 检查PyQt5是否安装
pip install PyQt5

# 检查其他依赖
pip install PyQt5 numpy pandas
```

### 树形图显示异常
- 尝试清空树后重新开始
- 检查终端是否有错误日志

### 搜索卡住
- 点击 **"⏹ 停止"** 按钮
- 检查API Key是否有效
- 降低搜索预算重试

## 💡 提示

1. **初次使用**: 建议先用"不指定类型"测试系统
2. **搜索预算**: 预算越大，发现优质因子的概率越高，但耗时越长
3. **观察模式**: 可以暂停观察当前树的结构，再继续搜索
4. **多次运行**: 每次运行会生成不同的搜索树，建议多次尝试

## 📊 与传统main.py的区别

| 特性 | main.py (命令行) | mcts_gui.py (GUI) |
|------|-----------------|-------------------|
| 交互性 | 低 | 高 |
| 可视化 | 无 | 实时搜索树 |
| 日志查看 | 滚动终端 | 彩色高亮 |
| 节点详情 | 无法查看 | 悬停/点击显示 |
| 操作便捷性 | 一般 | 一键操作 |
| 适用场景 | 批量运行 | 教学演示 |

## 📞 技术支持

如有问题，请检查：
1. 数据文件路径是否正确（`D:/AAProject/Data/000300SH.csv`）
2. API Key是否配置正确
3. 依赖是否完整安装

---

**注意**: GUI系统适合教学演示和单因子挖掘实验，批量实验仍建议使用 `main.py` 或 `run_experiment.py`。
