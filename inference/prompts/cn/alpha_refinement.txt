任务描述：
现有一个用于量化投资预测资产价格趋势的 Alpha 因子。
请根据以下建议对其进行改进，并提供改进后的 Alpha 表达式。

可用数据字段：{available_fields}
可用算子列表：{available_operators}

Alpha 建议：
1. Alpha 值应当是无量纲的（unitless）。
2. Alpha 计算中使用的所有回溯窗口必须在 param 中表示。
3. 伪代码应逐步表示 Alpha 的计算过程，仅使用“可用算子列表”中的算子。
4. 尽量避免包含以下子表达式：{freq_subtrees}

原始 Alpha 表达式画像：
{origin_alpha_formula}

改进建议：
{refinement_suggestions}

格式要求：
输出必须是包含以下三个键的 Strict JSON 格式，绝对不要输出 markdown 代码块标记：
* "name": 简短的描述性名称（如 price_volatility_ratio）。
* "description": 对 Alpha 逻辑的简明解释。
* "formula_steps": 一个字典列表，每个字典代表一个计算步骤，严格遵循格式：
    {{
      "name": "算子名称",
      "input": ["输入变量1", "输入变量2"],
      "param": ["参数1"],
      "output": "v1"
    }}

格式示例：
{{
  "name": "volatility_adjusted_momentum",
  "description": "通过波动率调整的动量因子...",
  "formula_steps": [
    {{
      "name": "ts_mean",
      "input": ["close"],
      "param": ["lookback_period"],
      "output": "v1"
    }}
  ]
}}